{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:frontend": "pnpm --filter @workspace/techstock run build",
    "build:cloudflare": "pnpm run typecheck:libs && pnpm --filter @workspace/techstock run build",
    "deploy:cloudflare": "pnpm run build:cloudflare && wrangler deploy",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "prettier": "^3.9.4",
    "typescript": "~5.9.3"
  },
  "pnpm": {
    "overrides": {
      "drizzle-orm": "catalog:"
    }
  }
}
