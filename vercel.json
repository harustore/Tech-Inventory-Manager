{
  "installCommand": "corepack enable && pnpm install",
  "buildCommand": "pnpm --filter @workspace/techstock run build",
  "outputDirectory": "artifacts/techstock/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.ts"
    }
  ]
}
